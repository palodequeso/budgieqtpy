// frontend/app/page.tsx
"use client";

export default function Calendar({ profileId }: { profileId: string }) {
    return (
        <main className="flex flex-col items-center justify-center p-24">
            <h1 className="text-4xl font-bold mb-8">Calendar</h1>
        </main>
    );
}
